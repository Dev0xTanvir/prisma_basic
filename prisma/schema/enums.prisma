enum ActiveStatus {
    ACTIVE
    BLOCKED
}

enum ROLE {
    ADMIN
    USER
    AUTHOR
}

enum Poststatus {
    DRAFT
    PUBLISHED
    PRIVATE
}

enum Commentstatus {
    APPROVED
    REJECT
}

enum Subscriptionstatus {
    ACTIVE
    CENCELED
}
